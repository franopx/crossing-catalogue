name: crossing_catalogue
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.0

dependencies:
  cached_network_image: ^3.4.1
  flutter:
    sdk: flutter
  flutter_html: ^3.0.0
  google_fonts: ^6.2.1
  http: ^1.4.0
  logger: ^2.6.0
  sqflite: ^2.4.2
  url_launcher: ^6.3.2
  internet_connection_checker: ^1.0.0+1
  shared_preferences: ^2.5.3
  provider: ^6.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
